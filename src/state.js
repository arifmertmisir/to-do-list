export let projectListArr = [];
export let projects = [];

export function setProjectListArr(newArr) {
  projectListArr = newArr;
}

export function setProjects(newProjects) {
  projects = newProjects;
}
